#include <stdio.h>
#include <stdlib.h>
struct per{
	char nom[30];
	char prenom[30];
	int age;
	}
void rev(){
	int i;
	for (i = 0; i < n; i++){
		if (age[i] > 20) {
			age[i]  = 0
			};
		}
	}
}
void aff(){
	int i;
	int taille = sizeof(nom)
	for(i=0;i <taille;i++)
		print('le nom ',nom[i])
		print('le prenom ',prenom[i])
		print('age ',age[i])
}
void remp(){
	int n;
	int i;
	int cp = 1;  
	printf('enter le nber des personnes : ');

	if (n != 0 ou n > 0){
		for(i=0;i<n;i++){
			printf("Enter les info de personne ", cp ," : ");
			printf("le nom: ");
			printf("le prenom : ");
			printf("Age: ");
			cp += 1;
    }
    else 
		printf('le des persone invalide !!!!');
    }
}

int main(){
maxminvoid produit::maxmin() {
    float min = 0;
    float max = 0;
    float tmp = 0;
    int taille_3 = prixpro.size();
    for (int i = 0; i < taille_3; i++){
		tmp = prixpro[i]
		if(tp < tmp){
			min = tmp;
		}
		if (tp > tmp){
			min = tmp;
		}
		}
	
}

}
